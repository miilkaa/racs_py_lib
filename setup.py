from setuptools import setup, find_packages

setup(
    name="racs-py-lib",
    version="0.1.0",
    packages=find_packages(),
    install_requirements=[
        "requests"
    ],
    author="miilkaa",
    
)
