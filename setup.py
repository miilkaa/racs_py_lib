from setuptools import setup, find_packages

setup(
    name="racs-py-lib",
    version="0.1.1",
    packages=find_packages(),
    install_requirements=[
        "requests"
    ],
    author="miilkaa",
    
)
